import React, { Component } from 'react';
import { View, Text, ScrollView, Animated, Platform, StyleSheet } from 'react-native'

export default class VideoScreen extends Component {
  constructor(props) {
    super(props)
    this.state = {
    }
  }

  render() {
    return (
      <View style={{ width: G_WIDTH }}>

      </View>
    )
  }
}